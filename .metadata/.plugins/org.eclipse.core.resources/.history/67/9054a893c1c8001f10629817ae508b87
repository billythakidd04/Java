
public class main {

	public static void main(String[] args)
	{
		int [] primeTest = new int[1000];//creating an array to store integers 1-100
		
		
		for (int x=2;x<1000;x++)
		{
			primeTest[x-2] = x;	//fill array starting at element 0 with a value of 2
		}
		
		for (int x=0; x<primeTest.length; ++x)	//for loop using element x of primeTest[]
		{
			int test = primeTest[x];
			int divisor = x+2;
			
			while (test>primeTest.length)
			{
				int outcome = test%divisor;
				if (outcome<=1)
				{
					System.out.print(test+" ");
				}
				++test;
			}
		}
		
	}

}
